This ForgeIR program has an effect error (`EFFECT-001`). Repair the `.fir` so `forge check` is clean. You may declare `! { fs }` on the caller. Do not remove the `exists` call. Do not emit TypeScript.

Current source:

```fir
{{source}}
```
